namespace lor
{
    const constexpr unsigned int SCREEN_WIDTH = 800;
    const constexpr unsigned int SCREEN_HEIGHT = 600;

    const constexpr unsigned int CIRCLE_SIZE = 5;
    const constexpr unsigned int NB_OF_CIRCLES = SCREEN_HEIGHT / CIRCLE_SIZE;
}